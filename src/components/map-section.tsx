import { ShareSection } from "./share-section";

export const MapSection:React.FC = () => {
    return (
        <section>
            <article>
                Map
            </article>
            <ShareSection/>
        </section>
    )
}